#include<stdio.h>
int fib(int n)
{
    if(n==0)
    return 0;
else if(n==1)
return 1;
else
return(fib(n-1)+fib(n-2));
}
void main()
{
    int i,num;
    printf("enter the number ");
    scanf("%d",&num);
for(i=0;i<=num;i++)
{
    printf("%d \n",fib[i]);
}._.
}